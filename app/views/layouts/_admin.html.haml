= yield

- content_for :head do
  %title= t("administration")

- content_for :header do
  %h1= link_to "#{configuration['name']} - #{t('administration')}", admin_path

- content_for :sidebar do
  %ul
    %li= link_to User.model_name.human.pluralize, admin_path
    %li= link_to Section.model_name.human.pluralize, admin_sections_path
