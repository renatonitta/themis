.posts
  - @posts.each do |post|
    .post
      .title= raw post.title.to_html
      .user= post.author.name
      .date= post.created_at
      .body= raw post.body.to_html
    .comments
      = link_to("Add comment", section_post_path(post.section, post, :anchor => "disqus_thread"))
      = raw disqus_comment_counts
